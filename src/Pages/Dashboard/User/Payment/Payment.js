import { Typography } from '@mui/material';
import React from 'react';

const Payment = () => {
    return (
        <div>
            <Typography variant="h3" sx={{ textAlign: "center" }}>
                Payment system will add soon! <br /> Just wait some days!
            </Typography>
        </div>
    );
};

export default Payment;